spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/otus_data_jpa
    username: otus
    password: otus

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog.xml

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss

  jpa:
    show-sql: true
